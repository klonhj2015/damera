=======================
damera service overview
=======================
The damera service provides...

The damera service consists of the following components:

``damera-api`` service
  Accepts and responds to end user compute API calls...
